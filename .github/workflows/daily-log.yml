name: Setup Daily Log Entry

on:
  schedule:
    - cron: '17 0 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  add-daily-line:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Insert Collapsible Daily Log and Update README
        run: |
          # 1. 한국 시간 기준 날짜 및 월 구하기
          MONTH_PATH=$(date -u -d "+9 hours" "+%Y-%m")
          TODAY=$(date -u -d "+9 hours" "+%Y-%m-%d (%a)")
          FILENAME="logs/daily-log-${MONTH_PATH}.md"
          
          # 2. logs 폴더 생성
          mkdir -p logs
          
          # 3. 월별 로그 파일 생성 (처음 한 번만)
          if [ ! -f "$FILENAME" ]; then
            echo "# 📅 $MONTH_PATH Learning Log" > "$FILENAME"
            echo -e "이번 달의 성장을 기록합니다. 🌱\n" >> "$FILENAME"
          fi

          # 4. 접이식(details) 구조로 오늘의 섹션을 파일 끝에 추가 (색상 제거 버전)
          echo -e "\n<details>" >> "$FILENAME"
          echo "<summary>🌱 <b>$TODAY</b></summary>" >> "$FILENAME"
          echo -e "<div markdown=\"1\">\n<br>\n" >> "$FILENAME"
          echo "- " >> "$FILENAME"
          echo -e "\n</div>" >> "$FILENAME"
          echo "</details>" >> "$FILENAME"

          # 5. README.md 링크 업데이트
          sed -i "s|\[📅 이번 달 로그 확인하기\](.*)|\[📅 이번 달 로그 확인하기\](./${FILENAME})|g" README.md

      - name: Commit and Push
        run: |
          git config --global user.name "kangsangmoon"
          git config --global user.email "sangmoonkang@naver.com"
          
          git add logs/ README.md
          
          TODAY=$(date -u -d "+9 hours" "+%Y-%m-%d (%a)")
          git commit -m "chore: revert to clean style daily log for $TODAY" || echo "No changes to commit"
          git push
